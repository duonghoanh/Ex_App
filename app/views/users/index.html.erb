<% provide(:title, 'All users') %>

<h1>All users</h1>
<%= paginate(@users, window: 2) %>
<ul class="users">
  <%# <%= render @users %>
  <%= render @users %>

</ul>
<%= paginate(@users, window: 2) %>